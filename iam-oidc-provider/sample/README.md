# iam-oidc-provider sample

Creates the GitHub Actions OIDC provider with local state. An account holds one
provider per issuer, so in an account that already has one, only plan:

```bash
terraform init
terraform plan
```
