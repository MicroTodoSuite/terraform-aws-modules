# route53-zone sample

Plans a public zone for `sample.microtodosuite.example` with local state. The
zone carries `prevent_destroy`, so the sample is meant to be planned:

```bash
terraform init
terraform plan
```
