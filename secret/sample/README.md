# secret sample

Plans the container `lex-mts-shd-sm-sample`, without a value, with local state.
The secret carries `prevent_destroy`, so the sample is meant to be planned:

```bash
terraform init
terraform plan
```
