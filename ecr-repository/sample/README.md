# ecr-repository sample

Plans one repository, `lex-mts-shd-ecr-authapi`, with local state. The
repository carries `prevent_destroy`, so the sample is meant to be planned:

```bash
terraform init
terraform plan
```
